import styled from "styled-components";

export const Label = styled.h2`
  margin: 0;

  font-family: "Rubik Glitch", system-ui;
  font-weight: 400;
  font-style: normal;

  font-size: 40px;
  line-height: 28px;
  text-align: center;

  color: #3ba4a9;

  cursor: default;

  user-select: none;
`;
